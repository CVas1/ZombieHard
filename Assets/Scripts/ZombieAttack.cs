using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ZombieAttack : MonoBehaviour
{
    public void Attack(GameObject target, int damage)
    {
        // Implement zombie attack logic on the target GameObject with the provided damage
        // For example, target.GetComponent<Player>().TakeDamage(damage);
    }
}
